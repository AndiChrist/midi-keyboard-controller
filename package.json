{
  "name": "midi-keyboard-controller",
  "version": "1.0.0",
  "description": "MIDI Keyboard Event Handler for AKAI MPK Mini",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npm run build && npm start",
    "dev-ts": "ts-node --skip-project src/index.ts",
    "watch": "tsc -w"
  },
  "keywords": ["midi", "keyboard", "akai", "typescript"],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "midi": "^2.0.0",
    "speaker": "^0.5.4",
    "chalk": "^4.1.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  }
}